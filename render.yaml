services:
  - type: web
    name: ofw-rag-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn functions.rag_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: CHROMA_DB_PATH
        value: /opt/render/project/src/kapwa_companion/chroma_db